{
  "name": "installment-tracker",
  "version": "2.0.0",
  "description": "Credit card installment payment tracker with cloud sync",
  "scripts": {
    "serve": "firebase serve",
    "deploy": "firebase deploy",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:firestore": "firebase deploy --only firestore:rules"
  },
  "keywords": [
    "installments",
    "payments",
    "tracker",
    "firebase",
    "credit-card"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}

